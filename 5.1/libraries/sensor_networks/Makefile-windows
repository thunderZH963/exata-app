#
# Define Sensor Networks library models.
#

include ../libraries/sensor_networks/Makefile-common

ADDON_OPTIONS   = $(ADDON_OPTIONS) $(SENSOR_NETWORKS_OPTIONS)
ADDON_SRCS      = $(ADDON_SRCS) $(SENSOR_NETWORKS_SRCS)
ADDON_INCLUDES  = $(ADDON_INCLUDES) $(SENSOR_NETWORKS_INCLUDES)

